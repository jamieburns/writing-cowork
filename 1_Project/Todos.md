# Todos — writing-cowork (plugin development)

Created: 2026-07-29

Granular task list for this repo. One row per work item. Distinct from
`2_Development/RoadMap/Roadmap.md`: the roadmap captures version status; this
captures the work inside it. Distinct from `1_Project/Decisions.md`: that
records commitments already made; this records work not yet done.

Task IDs are 8-char short hashes (locked decision #9). Status values:
`planned` → `in-progress` → `done`. Assignee `user` means Jamie does it
outside a session; everything else is agent-executable.

**Schema note.** This uses the plugin's own `pm-init-todos` schema so this repo
dogfoods `pm-add-task` / `pm-update-task` / `pm-list-tasks` / `pm-close-task`.
See task `a4e1c7b2` — `drift_check.py` expects a *different* column set, which
is a real defect surfaced by adopting the schema here.

## Now — blocking the v0.1.17 release

| ID | Description | Milestone | Assignee | Status | Added | Notes |
|----|-------------|-----------|----------|--------|-------|-------|
| 3f8a2d19 | Marketplace catalog description in `cowork-plugins-marketplace` | v0.1.17-release | user | planned | 2026-07-29 | Not a connected folder; deliberately untouched by sessions |
| 7c1b9e04 | Cowork-side marketplace refresh | v0.1.17-release | user | planned | 2026-07-29 | **Procedure simplified 2026-08-12** after retest on a throwaway plugin (`refresh-probe`, not shipped product) — see `Decisions.md`. New procedure: push the bump, update the *marketplace* first (not the plugin), wait for sync, verify from a brand-new context. No quit/relaunch/uninstall needed. Reproduced twice on the throwaway. **Not yet confirmed on the real writing-cowork plugin/jamie-cowork-plugins marketplace** — this row stays open until the actual v0.1.17 release attempt confirms the simplified procedure holds there too. Full steps in `1_Project/Process/dev-workflow-and-release.md` |
| b25d6f37 | Optional `release/v0.1.17-*` tag | v0.1.17-release | user | planned | 2026-07-29 | Matches `release/v0.1.14` |
| 2a6ccd37 | `pm-version` expected-sentinel block is stale - four values wrong, not two | v0.1.17-release | pm | done | 2026-08-04 | Found 2026-08-04. Body block claimed `version=0.1.15`, `skill count = 60`, `drift_check.py >= 0.2.0`, `default --by = milestone`; actual is **0.1.17, 66, 0.5.1, and `status`**. A correct v0.1.17 install would report `STALE` for two independent reasons. This is the instrument for verifying `7c1b9e04`, so it had to be right first. It had already caused a wrong belief - the morning session read `v0.1.13` off the description and recorded that as the loaded version; the synced package's own plugin.json says **0.1.14**. **DONE 2026-08-04:** all four values corrected, the `--by` sentinel now says read the file rather than asserting a value, and MAINTENANCE now covers the body block as well as the description marker |
| e90c4a58 | Run `pm-install-git-hooks` against this repo — first execution ever | v0.1.17-release | pm | done | 2026-07-29 | Hook has never fired. Verify exec bit, `core.hooksPath`, and that `drift_check.py --dry-run` emits "Session hygiene" lines. **DONE 2026-07-31:** installed to `1_Project/Process/git-hooks`, `core.hooksPath` set, exec bit verified, smoke test passed, and confirmed firing on a real commit (`a2e0c5e` caught 3 uncommitted files) |
| 14a7b3e6 | Verify memory settings actually gate, in a fresh Cowork session | v0.1.17-release | pm | done | 2026-07-29 | Cannot be tested from a session that already loaded them. #40495 suggests they may be inert. **DONE 2026-08-04 — answer is NO, they do not gate.** Observed turn-one in a fresh Cowork session: a memory index loaded despite `autoMemoryEnabled: false`, and its content was the hidden store's retirement note, not `1_Project/Memory/INDEX.md` — so neither key bound. Presence-evidence, robust to context truncation. Refinement: Cowork memory runs through the desktop-bridge MCP store, a different subsystem these Claude Code keys have no jurisdiction over — so no value of either key will help, and the row-1 retest is struck. Detail in `memory_gating_test_2026-07-25.md` |
| 0d92f4a7 | Install `drift_check.yaml` for this repo — it has none, so `pm-install-git-hooks` precondition 4 fails and the post-commit hook would be inert | v0.1.17-release | pm | done | 2026-07-29 | Blocks `e90c4a58`. Found 2026-07-29 trying to run the checker here; this repo is not registered with a drift config at all. **DONE 2026-07-31:** `1_Project/Process/drift_check.yaml` hand-authored for this repo's layout — not at `process/data_management/`, which `Decisions.md` forbids here |
| c3f80b6e | **The post-commit hook is silent when `drift_check.py` cannot run.** `drift_check.py:60-64` writes "ERROR: PyYAML not installed" to **stderr** and exits 2; `templates/hooks/post-commit:38` runs it with `2>/dev/null` and greps stdout for 'session hygiene'. Result: empty output, hook prints nothing, commit looks clean | v0.1.17-release | pm | done | 2026-07-30 | Found 2026-07-30 on the first real host run. Host python3 is `/opt/homebrew/bin/python3` and has **no PyYAML**, so the hook is inert on Jamie's own machine right now. Fifth instance of the silent-no-op class — and it is in the release-blocking component itself. **DONE 2026-07-31:** 4 silent exits in the hook now report; `drift_check` 0.4.0 reports unconfigured hygiene; `pm-install-git-hooks` gained a PyYAML precondition + smoke test. 5 failure modes verified on host |

## Next — v0.1.18, recovered from the dated handoffs

Verified against source 2026-07-29, not inherited on trust. Provenance:
`1_Project/Handoff/2026-07-2{1,2,3}_*.md` and
`1_Project/Documents/v0.1.15_planning_roadmap_2026-07-22.md`.

| ID | Description | Milestone | Assignee | Status | Added | Notes |
|----|-------------|-----------|----------|--------|-------|-------|
| a4e1c7b2 | Reconcile the todos schema: `drift_check.py:496` expects `id\|title\|status\|done\|milestone\|depends-on`; `pm-init-todos` writes `ID\|Description\|Milestone\|Assignee\|Status\|Added\|Notes` | v0.1.18 | pm | done | 2026-07-29 | **DONE 2026-07-31:** checker now reads the todos header row and maps columns by name, so it parses what `pm-init-todos` writes. Verified on this repo's 33 rows: 33/33 correct, previously 0/33. Found by adopting the schema in this repo. Any checker reading todos rows is operating on a schema no template produces |
| 7e4b1a93 | `drift_check.py` hardcodes consumer-layout paths: `config.vault / "process/active/todos.md"` (lines 544, 554) and `"process/active/roadmap.md"` (line 587). Not config keys — no vault on a different layout can point the todos/roadmap checks anywhere | v0.1.18 | pm | done | 2026-07-30 | Found 2026-07-30 scoping `0d92f4a7`. Distinct from `a4e1c7b2` (columns) and `c6d05a91` (inert check) — same *class*: the checker returns "clean" instead of "input not found". **2026-07-31:** drift_check 0.4.0 preflight now *reports* both paths as unreachable, so the silence is gone; the hardcoding itself is still open. **DONE 2026-07-31:** `todos:` and `roadmap:` are config keys as of drift_check 0.5.0, defaulting to the old hardcoded paths. This repo now points them at its real files |
| d51c9a27 | Correct the "read-only inspection is fine anywhere" clause in the router invariant — `git status` creates `.git/index.lock` and orphans it from a mounted view. Source is the MANAGED block, so fix `0_Product/skills/pm-install-router/SKILL.md` and `templates/CLAUDE.md`, not `CLAUDE.md` directly | v0.1.18 | pm | done | 2026-07-30 | Hand-editing `CLAUDE.md` would be silently reverted on the next sync. Evidence in `1_Project/Memory/feedback_git_status_creates_index_lock.md`. **DONE 2026-07-31:** corrected in `CLAUDE.md`, and a generic form added to `templates/CLAUDE.md` so consumer vaults get it and a sync cannot drop it |
| 9c14be7f | This repo's `CLAUDE.md` MANAGED block has **diverged from `templates/CLAUDE.md`** — it carries repo-specific invariants (osascript git lane, stage-a-file-once, review `git diff`, develop in CLI, version bump touches three places) that the template does not. By the marker convention, a `pm-install-router` sync regenerates MANAGED content and would silently drop all of them | v0.1.18 | pm | planned | 2026-07-31 | Found 2026-07-31 doing `d51c9a27`, which assumed the text lived in the template; it does not. Either the invariants move into the template, or they move to the PROJECT-OWNED section, or `pm-install-router` learns to merge. Note Decisions.md already records "pm-install-router — merge required" |
| 6b91e2a5 | Build the `priority_prefixes` triage mechanism in `drift_check.py` — findings under a priority prefix surface in a distinct "core product drift" section; everything else still scanned, just lower-priority | v0.1.18 | pm | planned | 2026-07-29 | **Reclassified 2026-07-30 from user-decision to build.** The note "mechanism confirmed 2026-07-22" misread the source: `v0.1.15_planning_roadmap_2026-07-22.md:207` confirmed the decision *to build* it. `grep priority_prefix` over `drift_check.py` returns zero hits — the key does not exist and is silently ignored if written into a yaml. Jamie's list is settled (2026-07-30) and recorded in `Decisions.md`; it is the spec, not a config value yet |
| c6d05a91 | Cross-phase dependency check is inert by default — `drift_check.py:536` gates on `cross_phase_config`, populated only from a `checks:` block the shipped `drift_check.yaml` template lacks | v0.1.18 | pm | done | 2026-07-29 | Shipped-but-dead code. Original finding 2026-07-23, re-verified 2026-07-29. **DONE 2026-07-31:** `checks:` block ships in the template. Also fixed the reason it would still have found nothing: `parse_roadmap_phases` scanned for `[#id]` links that neither roadmap template contains, so phases now derive from the Milestone column |
| 5b3e8c72 | Dependency gate at task close — `pm-close-task` should refuse or warn when `depends-on` is unmet | v0.1.18 | pm | planned | 2026-07-29 | Blocked by `a4e1c7b2`. Earlier claim that `depends-on` doesn't exist is stale — it exists in the checker, not the template |
| 8a45c0e3 | `pm-migrate-todos-schema` — legacy checkbox `todos.md` blocks task CRUD | v0.1.18 | pm | done | 2026-07-29 | Self-documented in `pm-list-tasks`; composite-kanban special-cases it. Affects Reconciliation Hypothesis. **DONE 2026-07-31:** `pm-migrate-todos-schema` shipped. Idempotent, refuses on a dirty working tree, derives stable IDs from description text, escapes pipes in cell content. `pm-list-tasks`' warning now names it instead of pointing at a roadmap milestone |
| 2d7f6b18 | Decide: drift exclude-list currency is per-vault config maintenance, or a plugin-level self-diagnosing mode | v0.1.18 | pm | planned | 2026-07-29 | Decision, not build. Outcome goes to `Decisions.md` |
| 2e169531 | Fix the corrupted `a4e1c7b2` row - an unescaped pipe shifted its columns and its Status still reads `planned` though the work is done | v0.1.18 | pm | done | 2026-08-04 | **DONE 2026-08-11:** re-escaped the pipes inside `a4e1c7b2`'s Description cell, moved its fused `**DONE 2026-07-31:**` completion text out of the middle of the Description and into Notes (prepended, matching the pattern used elsewhere in this table), and set its Status to `done`. The exact defect `pm-migrate-todos-schema` escapes for and `drift_check` 0.5.1 fixed, present in this repo's own todos. Unblocks `5b3e8c72`, which names `a4e1c7b2` as its blocker |
| 7a467f95 | Dangling skill references in shipping files - `pm-version` and `pm-sync-project-to-plugin` name `pm-migrate-to-shared-tool`, which no longer exists | v0.1.18 | pm | planned | 2026-08-04 | Add a `drift_check` rule resolving every `pm-*` / `voice-*` name in shipping files against `0_Product/skills/`. Same class as `a4e1c7b2`: nothing reported the broken pointer. Sharpened 2026-08-04: the stale name is also live in Cowork's loaded skill listing right now, because the synced snapshot predates the deletion |
| 3d9ef0dc | Settle gating-test row 4 - does Cowork auto-load repo-root `CLAUDE.md`? Must be asked on the FIRST turn of a new session | v0.1.18 | pm | planned | 2026-08-04 | An in-session agent cannot validly report a negative about session-start state. Two absence-based attempts have now failed (2026-08-04 morning, retracted; 2026-08-04 evening, turn one but still absence-based). Use a positive probe: ask for a fact only `CLAUDE.md` contains and forbid a file read. Procedure in `memory_gating_test_2026-07-25.md`. Blocks any router decision; touches `pm-install-router`, `templates/CLAUDE.md`, and `9c14be7f` |
| d8b5c359 | Verify at the next successful sync that the shipped package root is clean - no `ASSESSMENT_*`, `DECISIONS_v014.md`, `HANDOFF*`, `sync-mobile-*.md` | v0.1.18 | pm | planned | 2026-08-04 | **Reworded 2026-08-04.** The artifacts are present in the synced package, but that package is **0.1.14**, which predates the v0.1.15 root move - so this is an old defect still being served, not evidence of a live one. Cannot be checked against 0.1.17 until a refresh lands, which makes it downstream of `7c1b9e04`, not independent of it |
| f13c8d40 | Full Disk Access / launchd + iCloud fragility in `pm-setup-project` (macOS) | v0.1.18 | pm | planned | 2026-07-29 | Silent-fail class; has already caused a real outage per project memory |

## Next — decisions needed from Jamie

| ID | Description | Milestone | Assignee | Status | Added | Notes |
|----|-------------|-----------|----------|--------|-------|-------|
| a15f9d30 | Decide the status of the develop-in-CLI rule - it is stated as an invariant in CLAUDE.md but has no Decisions.md anchor | v0.1.18 | user | planned | 2026-08-04 | Found 2026-08-04. `CLAUDE.md:52` states *Develop in Claude Code CLI; Cowork is the runtime consumer. Never develop plugin changes from inside Cowork.* It entered in commit `953e588`, a memory-reconciliation commit that also created the router - not in a commit about dev workflow. `grep` of `Decisions.md` returns **zero** hits. Every other router invariant has an anchor; this one does not. The nearest sources are *descriptive* - `memory_management_recommendation_2026-07-25.md:73` and `Decisions.md:122` both say Jamie develops in CLI as an observation, used as an argument for runtime-independent controls. An agent appears to have hardened a habit into a prohibition. Decide: keep as an invariant with an anchor, downgrade to a scoped guideline, or drop. Note it sits inside the MANAGED block, so `9c14be7f` applies either way |
| d84a0c63 | Disposition the four dated handoffs in `1_Project/Handoff/` — frozen history or delete | v0.1.18 | user | planned | 2026-07-29 | Everything unresolved in them is now on this list, so nothing is lost either way |
| 0c5e93b7 | Confirm `resources/` content policy is a per-vault `charter.md` call and write the closing note | v0.1.18 | user | planned | 2026-07-29 | Already reasoned through 2026-07-22; needs a sentence, not design work |
| 47f2a8d1 | Decide whether the matcher false-positive is worth pursuing at all | v0.1.18 | user | planned | 2026-07-29 | Its sourcing was struck 2026-07-22 (misattributed from Epistemology). Recommend dropping to a theoretical note |

## Later — carried backlog

| ID | Description | Milestone | Assignee | Status | Added | Notes |
|----|-------------|-----------|----------|--------|-------|-------|
| ba6d3f19 | Review Skills — Subset 4, 13 skills | backlog | pm | planned | 2026-07-29 | Locked in `DECISIONS_v014.md`, never shipped. Scope in `2_Development/RoadMap/v0.1.14/ASSESSMENT_REVIEW_SKILLS_v014.md` |
| 71e4c8b0 | Drift Enhancements — verify what actually shipped before rebuilding | backlog | pm | planned | 2026-07-29 | Status-staleness timestamps and `.gitkeep` fix may already be in. Scope in `ASSESSMENT_DRIFT_ENHANCEMENTS_v014.md`. Overlaps `c6d05a91` |
| 3ca70e52 | `#63081` — replace angle brackets in 16 skill `description:` fields (`<vault>` → `[vault]`) | backlog | pm | done | 2026-07-29 | Cowork's upload validator fails silently on these. Low urgency: the CLI dev workflow doesn't hit it. **DONE 2026-07-31:** 16 skills rewritten `<x>` -> `[x]`, description field only; all 62 skill frontmatters still parse as valid YAML |
| e58b21fc | Citation ledger — design the `last accessed` touch mechanism | backlog | pm | planned | 2026-07-29 | Load-bearing: the 16-day TTL is locked, so an unreliable touch means wrong deletions. Nothing built yet — no template, no skill |
| 96d7c34a | Citation ledger — `last verified` staleness checking | backlog | pm | planned | 2026-07-29 | Manual for now; automatic deferred. Blocked by `e58b21fc` |
| 4e0b9a76 | Missing `analysis-*` and review-tracking skill families | backlog | pm | planned | 2026-07-29 | Causes ad hoc, inconsistent workarounds per the 2026-07-21 problem report |
| c72f5e81 | Batch / preview operations on the claim → promote → process-inbox chain | backlog | pm | planned | 2026-07-29 | Needs usage evidence first: how often are multi-file promotions actually done? |
| 1f6a4d90 | `pm-release-file` — distinguish force-releasing someone else's stale claim from a normal self-release | backlog | pm | done | 2026-07-29 | Same code path today; an accidental unlock leaves no audit trace. **DONE 2026-07-31:** `pm-release-file` 0.2.0 — `--as=` makes self-vs-other decidable, releasing another context's claim requires `--force`, and a forced release appends `force-released from X by Y [date]` to the row's Notes |
| 5a91b6e2 | `git log --all` sweep to convert the skill usage-signal doc from signal to ledger | backlog | pm | planned | 2026-07-29 | Would settle the "apparently unused" skills question and the `writer_voice_sample*.md` provenance question |
| 20c8f7d5 | Recheck Anthropic support thread 215474352137566 | backlog | user | planned | 2026-07-29 | Four items, all open as of 2026-07-21. Recheck, don't re-research — record in `history/2026-07-29_roadmap-narrative-archive.md`. **Evidence added 2026-08-04:** `/root/.claude/plugins/synced/manifest.json` shows `writing-cowork` with `"version": null` and `updatedAt: 2026-05-24T19:42:40Z` - frozen ten weeks, and carrying no version to compare against. Stronger: the same manifest shows `cowork-plugin-management` updated **2026-08-04T00:17**, so the sync machinery is working and is frozen for this one plugin only. The synced package is 0.1.14 with 60 skills |
| b47e1a38 | v0.1.19 — production-pipeline tooling | v0.1.19 | pm | planned | 2026-07-29 | Deferred |
| e6c2d509 | v0.2.0 — `pm-sync-project-to-plugin`: drift-check config + vault layout + router migration | v0.2.0 | pm | planned | 2026-07-29 | Deferred. Skill exists at v0.2.0 with the layout case; the sync mechanism is the remaining work |
| 71989d02 | Review Skills — Subset 5 (Substance, 3 skills) | backlog | user | planned | 2026-08-04 | Recovered 2026-08-04 from the retired `2_Development/RoadMap/Todo-list.md` — the only item in that file not already carried here. Marked *deferred, user handling separately* on 2026-07-21; status unverified since. Sibling of `ba6d3f19` (Subset 4). Scope in `DECISIONS_v014.md` |
| 10f186b7 | Skill inventory reduction - 7 removals plus demoting the bootstrap family to one orchestrator | v0.1.19 | pm | planned | 2026-08-04 | Deferred 2026-08-04 pending the v0.1.17 release. Full spec, ripple edits and sequencing in `1_Project/Documents/2026-07-31_skill-inventory-reduction.md`. Requires a `Decisions.md` entry retiring the v0.1.14 kanban lock. **Baseline corrected 2026-08-04:** the doc measured the repo at 0.1.17 (66 skills, ~6,700 tokens/turn), which Cowork is not running. Measured from the actually-loaded snapshot: **60 skills, 24,914 chars, ~6,230 tokens/turn, bootstrap family 21 skills / ~2,640 tokens / 42%**. Six of the eight most expensive entries are bootstrap, so the Tier-1 target holds. **Downstream of `7c1b9e04`** - until a refresh lands, deleting skills changes the repo and not the token bill |
| a56c4e92 | Delete `refresh-probe` test scaffolding — `0100_TestProduct/` in this repo and the `jamieburns/refresh-probe-marketplace` GitHub repo | v0.1.17-release | user | planned | 2026-08-12 | Built to retest `7c1b9e04` without risking the real plugin or a working writing project — see `Decisions.md` 2026-08-12 entry. Two version-bump cycles confirmed the simplified refresh procedure works on the throwaway. Delete only after the real v0.1.17 release attempt confirms the same procedure holds for `writing-cowork`/`jamie-cowork-plugins` — don't delete the evidence before it's actually corroborated on the real thing |
